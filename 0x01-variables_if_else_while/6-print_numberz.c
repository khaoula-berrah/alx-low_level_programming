#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - Entry point
 *Explain: 'numberz 6'
 *Return: Always 0 (Success)
 */
int main(void)
{
	int r;

	for (r == 0; r <= 9; r++)
	{
		putchar(r + 48);
	}
	putchar('\n');
	return (0);
}
